# Core pipeline
torch>=2.0
numpy>=1.24
scipy>=1.10          # stats (bootstrap CIs, Poisson intervals) in the eval scripts
pillow>=9.0

# Demo
streamlit>=1.30
pandas>=2.0
altair>=5.0

# Optional — only for the pretrained-backbone generality probe (src/32)
# gdown>=5.0         # download the DeepPrint weights from Google Drive
